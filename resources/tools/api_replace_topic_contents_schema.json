{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ReplaceTopicContents",
  "description": "Replace a topic's ENTIRE markdown content. Prefer EditTopicContents for anything targeted. Use only when rewriting whole sections, changing >~50% of the file, or filling a freshly-created topic.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "contents"],
  "properties": {
    "id": {
      "type": "integer",
      "description": "ID of the topic to update."
    },
    "contents": {
      "type": "string",
      "description": "New markdown content; completely replaces the existing content."
    }
  }
}
