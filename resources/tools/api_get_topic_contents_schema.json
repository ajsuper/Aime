{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "GetTopicContents",
  "description": "Submit to POST /api. Retrieves the markdown content of a topic by its ID.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id"],
  "properties": {
    "id": {
      "type": ["integer", "string"],
      "description": "The ID of the topic whose content you want to retrieve. For your own topics this is the integer id. For a topic shared with you, pass the exact id string shown in the topic list (e.g. \"5:12\")."
    }
  }
}
