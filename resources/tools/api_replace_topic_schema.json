{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ReplaceTopic",
  "description": "Replace a topic's metadata. Does NOT touch its contents/.md file.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "summary", "category"],
  "properties": {
    "id": {
      "type": "integer",
      "minimum": 0,
      "description": "ID of the topic to update."
    },
    "summary": {
      "type": "string",
      "description": "Brief, keyword-rich summary for later lookup; keep it short."
    },
    "category": {
      "type": "string",
      "description": "Category; reuse an existing one where possible."
    },
    "folder": {
      "type": "string",
      "maxLength": 32,
      "description": "Optional folder (see the system prompt's Folders rules). Omit = leave unchanged; empty string = move to root. To rename a folder across topics, use RenameFolder. Max 32 bytes."
    }
  }
}
