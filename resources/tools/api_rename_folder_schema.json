{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RenameFolder",
  "description": "Rename a folder. Use this whenever a folder's name should change. To move a single topic between folders, use ReplaceTopic with the 'folder' field instead.",
  "type": "object",
  "additionalProperties": false,
  "required": ["old_name", "new_name"],
  "properties": {
    "old_name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 32,
      "description": "The current folder name. Case-insensitive."
    },
    "new_name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 32,
      "description": "The new folder name. Max 32 bytes; control characters and U+FFFD are rejected. If a folder with this name already exists, the two are merged."
    }
  }
}
