{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RenameFolder",
  "description": "Rename a folder. To move a single topic, use ReplaceTopic's 'folder' field instead.",
  "type": "object",
  "additionalProperties": false,
  "required": ["old_name", "new_name"],
  "properties": {
    "old_name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 32,
      "description": "Current folder name (case-insensitive)."
    },
    "new_name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 32,
      "description": "New name. Max 32 bytes. If it already exists, the two merge."
    }
  }
}
