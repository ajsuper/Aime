{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ListFolders",
  "description": "List every non-empty folder with its topic count. Cheap (names + counts only). Use before creating/moving a topic into a folder so you reuse names exactly.",
  "type": "object",
  "additionalProperties": false,
  "properties": {}
}
