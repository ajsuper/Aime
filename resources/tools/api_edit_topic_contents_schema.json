{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EditTopicContents",
  "description": "Preferred way to edit a topic: surgical find/replace patches, applied top-to-bottom. Each 'find' must match EXACTLY ONCE (if not, it fails with the count — widen and retry). See the system prompt's editing rules.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "patches"],
  "properties": {
    "id": {
      "type": ["integer", "string"],
      "description": "ID of the topic to edit. For your own topics this is the integer id. For a topic shared with you (edit permission required), pass the exact id string shown in the topic list (e.g. \"5:12\")."
    },
    "patches": {
      "type": "array",
      "description": "Patches applied sequentially; batch all edits into one call.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["find", "replace"],
        "properties": {
          "find": {
            "type": "string",
            "description": "Exact text to locate; must match once. Use \\n for newlines; include context to be unique."
          },
          "replace": {
            "type": "string",
            "description": "Replacement text. Use \\n for newlines."
          }
        }
      }
    }
  }
}
