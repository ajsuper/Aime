{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CreateEvent",
  "description": "Create a new event (ID assigned by the server). Use EditEvent to modify an existing one.",
  "type": "object",
  "additionalProperties": false,
  "required": ["date", "title", "summary", "category", "archived"],
  "properties": {
    "date": {
      "type": "string",
      "pattern": "^\\d{2}/\\d{2}/\\d{4}$",
      "description": "DD/MM/YYYY. For a multi-day event, the start date."
    },
    "time": {
      "type": "string",
      "pattern": "^\\d{2}:\\d{2}$",
      "description": "Start time, HH:MM. Optional."
    },
    "category": {
      "type": "string",
      "description": "One-word category; reuse an existing one where possible."
    },
    "archived": {
      "type": "boolean",
      "default": false,
      "description": "Soft-delete flag — this is the delete button. True hides the event from the default view entirely, for things that no longer matter at all (mistakes, dismissed plans). Do NOT use it for events that happened or were called off — set `status` to completed/canceled for those; they stay visible. Almost always false on create."
    },
    "summary": {
      "type": "string",
      "description": "Event details as GitHub-flavored Markdown (see the Events section of the system prompt for the markdown/checkbox conventions)."
    },
    "title": {
      "type": "string",
      "description": "Short title (≤20 chars)."
    },
    "commitment_id": {
      "type": "string",
      "description": "Optional stable slug shared by every recurring instance of the same commitment (e.g. \"bouldering\", \"sat_practice\"). Set it on anything recurring so patterns (cancellations, streaks) stay queryable. Reuse the exact slug across instances."
    },
    "status": {
      "type": "string",
      "enum": ["scheduled", "completed", "canceled", "unknown"],
      "default": "scheduled",
      "description": "Lifecycle state. New events are 'scheduled'. ('unknown' is set automatically by the system for past events left unresolved; don't set it yourself.)"
    },
    "status_change_reason": {
      "type": "string",
      "description": "Short freeform reason for the current status, whenever one is worth recording — e.g. why it was canceled (\"tired\", \"conflict\", \"AiMe\"), why it was moved, or a note on how it went when completed. Most useful for cancellations and reschedules."
    },
    "rescheduled_from": {
      "type": "string",
      "pattern": "^\\d{2}/\\d{2}/\\d{4}$",
      "description": "Original DD/MM/YYYY this instance was moved from. Set it (and use the new date) when an event is rescheduled; the status stays 'scheduled'."
    }
  }
}
