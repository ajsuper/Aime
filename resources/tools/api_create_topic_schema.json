{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CreateTopic",
  "description": "Create a new topic and its markdown file (ID assigned by the server).",
  "type": "object",
  "additionalProperties": false,
  "required": ["title", "summary", "category"],
  "properties": {
    "title": {
      "type": "string",
      "description": "A title for the topic."
    },
    "summary": {
      "type": "string",
      "description": "Brief, keyword-rich summary for later lookup; keep it short."
    },
    "category": {
      "type": "string",
      "description": "Category; reuse an existing one where possible."
    },
    "folder": {
      "type": "string",
      "maxLength": 32,
      "description": "Optional folder (see the system prompt's Folders rules). Omit or empty string = root. Max 32 bytes."
    }
  }
}
