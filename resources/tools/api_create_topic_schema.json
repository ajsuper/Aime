{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CreateTopic",
  "description": "Submit topic body for POST /api. Creates a new topic and generates a markdown file. The ID is assigned automatically by the server.",
  "type": "object",
  "additionalProperties": false,
  "required": ["title", "summary", "category"],
  "properties": {
    "title": {
      "type": "string",
      "description": "A title for the topic."
    },
    "summary": {
      "type": "string",
      "description": "A brief summary including keywords that make it easy to find later, but not take up too much context."
    },
    "category": {
      "type": "string",
      "description": "A category to help organize and identify topics. Please attempt to use categories that are already being used by other topics and only use a new category if you think it is needed."
    },
    "folder": {
      "type": "string",
      "maxLength": 32,
      "description": "Optional folder name this topic belongs to (e.g. 'Work', 'Health'). Folders exist implicitly — using a name no other topic uses creates that folder. Matching is case-insensitive: passing 'work' when a 'Work' folder already exists files this topic under 'Work' (the first-seen casing wins). Omit or pass an empty string to place the topic at the root; whitespace-only values are treated as empty. Max 32 bytes. Control characters and the Unicode replacement character (U+FFFD) are rejected."
    }
  }
}
