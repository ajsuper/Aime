{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WebSearch",
  "description": "Delegate a research task to a capable web-research assistant. This is NOT a search-query box — it is a message to another AI that runs as many web searches as it needs, reads the pages, and returns a digest plus a Sources list (you never see raw pages). Put your ENTIRE research need in ONE call: if you need the same kind of information for many items (e.g. tuition for 10 colleges, specs for 5 phones, hours for several stores), describe all of them in a single request and the assistant will research each — do NOT make one call per item. Be specific about what to find and how you want it back (dates, numbers, comparisons, a per-item breakdown). Cite the returned Sources when you use the findings.",
  "type": "object",
  "additionalProperties": false,
  "required": ["request"],
  "properties": {
    "request": {
      "type": "string",
      "description": "What to find, in detail — the question and any specifics you need back."
    }
  }
}
