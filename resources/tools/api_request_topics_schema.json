{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FilterTopics",
  "description": "Request body for POST /api. All fields are optional; omitted fields use their defaults. The tool gets a list of topics based on the filters.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "category": {
      "type": "string",
      "description": "Exact single-category match. Takes precedence over 'categories' if both are provided."
    },
    "categories": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "OR match across multiple categories. Ignored if 'category' is also set."
    },
    "keyword": {
      "type": "string",
      "description": "Case-insensitive substring search across topic title and summary."
    },
    "limit": {
      "type": "integer",
      "minimum": 0,
      "default": 0,
      "description": "Maximum number of results to return. 0 means unlimited."
    }
  }
}
