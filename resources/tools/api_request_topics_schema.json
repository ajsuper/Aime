{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FilterTopics",
  "description": "List topics. All fields optional. Each result includes its folder (empty = root); folder is not filterable — group client-side if needed.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "category": {
      "type": "string",
      "description": "Exact single-category match. Overrides 'categories'."
    },
    "categories": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "OR match across categories. Ignored if 'category' is set."
    },
    "keyword": {
      "type": "string",
      "description": "Case-insensitive substring search over title and summary."
    },
    "limit": {
      "type": "integer",
      "minimum": 0,
      "default": 0,
      "description": "Max results; 0 = unlimited."
    }
  }
}
