{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EditEvent",
  "description": "Replace an existing event. ALL fields required — repeat unchanged ones as-is.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "date", "title", "summary", "category", "archived"],
  "properties": {
    "date": {
      "type": "string",
      "pattern": "^\\d{2}/\\d{2}/\\d{4}$",
      "description": "DD/MM/YYYY start date. For a multi-day event, set end_date (or duration) for the end."
    },
    "time": {
      "type": "string",
      "pattern": "^\\d{2}:\\d{2}$",
      "description": "Start time, HH:MM. Optional."
    },
    "duration": {
      "type": "string",
      "description": "How long the event lasts; the server converts it to an absolute end. Compact form: '90m', '2h', '1h30m', '3d', '2w', '2mo', '1y'. Use this OR end_date/end_time, never both. To move an event AND keep its length, change `date`/`time` and pass `duration` again (the end is recomputed from the new start). To move it but anchor the end, repeat the existing end_date/end_time instead. Omit to leave the existing end unchanged; pass an empty end_date to clear the length."
    },
    "end_date": {
      "type": "string",
      "pattern": "^\\d{2}/\\d{2}/\\d{4}$",
      "description": "DD/MM/YYYY the event ends on. Optional; alternative to `duration`. Must be on or after `date`. This is an absolute end and does not move on its own. Omit to keep the existing value; pass empty string to clear it."
    },
    "end_time": {
      "type": "string",
      "pattern": "^\\d{2}:\\d{2}$",
      "description": "End time, HH:MM. Optional; requires `time`. Omit to keep the existing value; pass empty string to clear it."
    },
    "category": {
      "type": "string",
      "description": "One-word category; reuse an existing one where possible."
    },
    "archived": {
      "type": "boolean",
      "default": false,
      "description": "Soft-delete flag — this is the delete button. True hides the event from the default view entirely, for things that no longer matter at all (mistakes, dismissed plans). Do NOT use it to mark an event done or called off — set `status` to completed/canceled for those; they stay visible. Leave false unless the user genuinely wants the event gone."
    },
    "summary": {
      "type": "string",
      "description": "Event details as GitHub-flavored Markdown (see the Events section of the system prompt). Preserve existing checkbox state unless asked to change it."
    },
    "title": {
      "type": "string",
      "description": "Short title."
    },
    "id": {
      "type": "integer",
      "minimum": 0,
      "default": 0,
      "description": "ID of the event to replace."
    },
    "commitment_id": {
      "type": "string",
      "description": "Optional stable slug shared by every recurring instance of the same commitment (e.g. \"bouldering\"). Omit to keep the existing value; pass to set/change it. Reuse the exact slug across instances."
    },
    "status": {
      "type": "string",
      "enum": ["scheduled", "completed", "canceled", "unknown"],
      "description": "Lifecycle state. Set 'completed' when it happened, 'canceled' (with status_change_reason) when dropped. To move an event, change its date and set rescheduled_from to the old date — leave the status 'scheduled'. 'unknown' is set automatically by the system for past events you never resolved; don't set it yourself. Omit to keep the existing value."
    },
    "status_change_reason": {
      "type": "string",
      "description": "Short freeform reason for the current status, whenever one is worth recording — e.g. why it was canceled (\"tired\", \"conflict\", \"AiMe\"), why it was moved, or a note on how it went when completed. Most useful for cancellations and reschedules. Omit to keep the existing value."
    },
    "rescheduled_from": {
      "type": "string",
      "pattern": "^\\d{2}/\\d{2}/\\d{4}$",
      "description": "Original DD/MM/YYYY this instance was moved from. Set it (and update the date) when rescheduling an event; the status stays 'scheduled'. Omit to keep the existing value."
    }
  }
}
