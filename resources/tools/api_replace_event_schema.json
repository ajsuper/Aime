{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EditEvent",
  "description": "Replace an existing event. ALL fields required — repeat unchanged ones as-is.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "date", "title", "summary", "category", "archived"],
  "properties": {
    "date": {
      "type": "string",
      "pattern": "^\\d{2}/\\d{2}/\\d{4}$",
      "description": "DD/MM/YYYY. For a multi-day event, the start date."
    },
    "time": {
      "type": "string",
      "pattern": "^\\d{2}:\\d{2}$",
      "description": "Start time, HH:MM. Optional."
    },
    "category": {
      "type": "string",
      "description": "One-word category; reuse an existing one where possible."
    },
    "archived": {
      "type": "boolean",
      "default": false,
      "description": "Soft-delete flag — this is the delete button. True hides the event from the default view entirely, for things that no longer matter at all (mistakes, dismissed plans). Do NOT use it to mark an event done or called off — set `status` to completed/canceled for those; they stay visible. Leave false unless the user genuinely wants the event gone."
    },
    "summary": {
      "type": "string",
      "description": "Event details as GitHub-flavored Markdown (see the Events section of the system prompt). Preserve existing checkbox state unless asked to change it."
    },
    "title": {
      "type": "string",
      "description": "Short title."
    },
    "id": {
      "type": "integer",
      "minimum": 0,
      "default": 0,
      "description": "ID of the event to replace."
    },
    "commitment_id": {
      "type": "string",
      "description": "Optional stable slug shared by every recurring instance of the same commitment (e.g. \"bouldering\"). Omit to keep the existing value; pass to set/change it. Reuse the exact slug across instances."
    },
    "status": {
      "type": "string",
      "enum": ["scheduled", "completed", "canceled", "rescheduled"],
      "description": "Lifecycle state. Set 'completed' when it happened, 'canceled' (with cancel_reason) when dropped, 'rescheduled' (with rescheduled_from) when moved. Omit to keep the existing value."
    },
    "cancel_reason": {
      "type": "string",
      "description": "Short freeform reason, only when status is 'canceled' (e.g. \"tired\", \"conflict\", \"AiMe\"). Omit to keep the existing value."
    },
    "rescheduled_from": {
      "type": "string",
      "pattern": "^\\d{2}/\\d{2}/\\d{4}$",
      "description": "Original DD/MM/YYYY this instance was moved from, only when status is 'rescheduled'. Omit to keep the existing value."
    }
  }
}
