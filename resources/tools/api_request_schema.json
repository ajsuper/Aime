{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FilterUsersEvents",
  "description": "List the user's events. All fields optional. Always pass at least one filter — fetching everything wastes context.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "filter_by_date": {
      "type": "boolean",
      "default": false,
      "description": "Restrict to [start_date, end_date]."
    },
    "start_date": {
      "type": "string",
      "pattern": "^\\d{2}/\\d{2}/\\d{4}$",
      "default": "01/01/1970",
      "description": "Inclusive range start, DD/MM/YYYY. Used only if filter_by_date."
    },
    "end_date": {
      "type": "string",
      "pattern": "^\\d{2}/\\d{2}/\\d{4}$",
      "default": "31/12/9999",
      "description": "Inclusive range end, DD/MM/YYYY. Used only if filter_by_date."
    },
    "filter_by_time": {
      "type": "boolean",
      "default": false,
      "description": "Restrict to [start_time, end_time]."
    },
    "start_time": {
      "type": "string",
      "pattern": "^\\d{2}:\\d{2}$",
      "default": "00:00",
      "description": "Inclusive range start, HH:MM. Used only if filter_by_time."
    },
    "end_time": {
      "type": "string",
      "pattern": "^\\d{2}:\\d{2}$",
      "default": "23:59",
      "description": "Inclusive range end, HH:MM. Used only if filter_by_time."
    },
    "category": {
      "type": "string",
      "description": "Exact single-category match. Overrides 'categories'."
    },
    "categories": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "OR match across categories. Ignored if 'category' is set."
    },
    "keyword": {
      "type": "string",
      "description": "Case-insensitive substring search over title and summary."
    },
    "archived": {
      "type": "string",
      "enum": ["active_only", "archived_only", "all"],
      "default": "active_only",
      "description": "Filters on the soft-delete flag, NOT the lifecycle status. 'active_only' = not deleted (this still includes completed and canceled events — check their `status` field). 'archived_only' = only soft-deleted ones. 'all' = both; try it if you can't find something that may have been deleted."
    },
    "sort_order": {
      "type": "string",
      "enum": ["asc", "desc"],
      "default": "asc",
      "description": "Sort by date; ties broken by time ascending."
    },
    "limit": {
      "type": "integer",
      "minimum": 0,
      "default": 0,
      "description": "Max results; 0 = unlimited."
    }
  }
}
