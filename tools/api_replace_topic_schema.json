{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ReplaceTopic",
  "description": "Submit topic body for POST /api. Replaces the topic metadata. Does NOT replace the topic contents or .md file.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "summary", "category"],
  "properties": {
    "id": {
      "type": "integer",
      "minimum": 0,
      "description": "The ID of the topic to replace."
    },
    "summary": {
      "type": "string",
      "description": "A brief summary including keywords that make it easy to find later, but not take up too much context."
    },
    "category": {
      "type": "string",
      "description": "A category to help organize and identify topics. Please attempt to use categories that are already being used by other topics and only use a new category if you think it is needed."
    }
  }
}
