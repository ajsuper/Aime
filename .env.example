# Copy to .env and fill in. Used by docker-compose.yml. Do not commit .env.

# Required — your Anthropic API key. Do not share it with anyone.
ANTHROPIC_API_KEY=

# Server / TLS mode:
#   0 (default, recommended) — serve plain HTTP via the production waitress
#     server, with TLS terminated by a reverse proxy in front. http://localhost
#     is a browser "secure context", so the microphone works locally too.
#   1 — terminate self-signed TLS in the built-in (development) server, for
#     reaching the box directly without a proxy (e.g. LAN phone mic).
AIME_HTTPS=0

# Treat the browser-facing connection as HTTPS so the session cookie is marked
# Secure and HSTS is sent — keep this 1 whenever TLS terminates at a proxy in
# front (even with AIME_HTTPS=0). Set 0 only for plain-HTTP, non-localhost dev.
AIME_SECURE_COOKIES=1

# Worker threads for the waitress server (AIME_HTTPS=0). Each in-flight chat
# streams over one thread, so keep this above your expected concurrency.
AIME_THREADS=16

# Number of reverse-proxy hops between the client and the app. The app reads
# the real client IP (for rate limiting and the audit log) and the original
# scheme from X-Forwarded-For / X-Forwarded-Proto, but only trusts this many
# hops of those headers.
#   0 — reached directly, no proxy (loopback, or AIME_HTTPS=1 direct mode).
#   1 — one proxy in front (a single Caddy/nginx). This is the default.
#   2 — two proxies (e.g. Cloudflare -> Caddy -> app).
# SECURITY: set this to the EXACT number of proxies you actually run. Trusting
# more hops than exist lets a client forge X-Forwarded-For to spoof its source
# IP — bypassing the per-IP rate limits and poisoning the audit log.
AIME_TRUSTED_PROXY_HOPS=1

# Address the web app binds to. Used by the native install path
# (scripts/web_app_serve.sh). 127.0.0.1 = host-only; 0.0.0.0 = all interfaces.
# The Docker path always binds 0.0.0.0 inside the container regardless.
AIME_BIND=127.0.0.1

# Allow new account creation (1) or run closed so only the admin can add
# users (0). Default closed.
AIME_ALLOW_SIGNUP=0

# Access mode (the safe default is "keys").
#   keys = a user must redeem a single-use invite key before they can send
#          messages. Mint keys with: scripts/access_keys.py gen
#   open = no send-gate; any account can message immediately. Local /
#          fully-trusted use only.
# See docs/access-control.md.
AIME_ACCESS_MODE=keys

# Collect per-call API token usage + local STT compute to usage.jsonl (1/0).
AIME_USAGE_STATS=0

# When usage stats are on, tag each record with the username (1) or keep it
# anonymous (0).
AIME_USAGE_LINK_USERS=0

# Run the admin dashboard alongside the web app (1) or not (0). When on, it is
# served at http://localhost:5050 (HTTP, host-loopback only). It shows usage
# statistics and manages user accounts and invite keys (a web version of the
# scripts/ admin tools). AIME_USAGE_STATS=1 fills in the usage tabs.
AIME_USAGE_DASHBOARD=0

# Password for the admin dashboard. REQUIRED whenever AIME_USAGE_DASHBOARD=1 —
# the dashboard can disable accounts and spend money, so it refuses to start
# without one. Choose a strong value; it is the only thing protecting it.
AIME_ADMIN_PASSWORD=

# Email-based two-factor auth. 1 = on, 0 = off (default).
# When on, every sign-in is a full 2FA: after the password is accepted a
# 6-digit code is emailed to the account and must be entered to finish logging
# in. Signup also requires verifying an email. Accounts with no email on file
# yet (e.g. created before this was enabled) are prompted to add and verify one
# the next time they log in, after which every later login is challenged.
# When off, the email field is hidden on signup and no codes are sent; the
# email-related code paths are inert.
# Requires the SMTP settings above to be configured.
DO_EMAIL_VERIFICATION=0

# SMTP settings used to send email (e.g. for two-factor auth codes).
# Required when DO_EMAIL_VERIFICATION=1.
#
# Simplest case (a single Gmail-style account): just set EMAIL_ADDRESS and
# EMAIL_PASSWORD. The address is used both to log in and as the From line, and
# host/port default to Gmail's submission endpoint.
EMAIL_ADDRESS=
EMAIL_PASSWORD=
#
# More involved setups (a custom provider, or a relay whose login username
# differs from the visible From address — e.g. SendGrid's "apikey" user) can
# set these explicitly. Each falls back to the EMAIL_ADDRESS/EMAIL_PASSWORD
# pair above when left blank, so you only override what you need.
#   SMTP_HOST      server hostname            (default smtp.gmail.com)
#   SMTP_PORT      submission port, STARTTLS  (default 587)
#   SMTP_USERNAME  login username             (default EMAIL_ADDRESS)
#   SMTP_PASSWORD  login password             (default EMAIL_PASSWORD)
#   SMTP_FROM      visible From address       (default SMTP_USERNAME)
SMTP_HOST=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=

# --- Outbound messaging (aime.messaging) -------------------------------------
# Lets Aime and background agents text the user outside the chat: a reminder
# coming due, a finished background task, a follow-up answer. The SendMessage
# tool and the agent SubmitResult `message_to_user` field both route through
# here. AIME_MESSAGING=0 disables sending (the tool becomes inert).
AIME_MESSAGING=1
# Transport to use: "telegram" (default, free, easiest to prototype) or "email"
# (reuses the SMTP creds above). A production SMS channel can be added later and
# selected here without changing any calling code.
AIME_MESSAGING_CHANNEL=telegram
# Telegram setup: message @BotFather, send /newbot, paste the token below.
# Then message your new bot once and read your numeric chat id from
# https://api.telegram.org/bot<token>/getUpdates
TELEGRAM_BOT_TOKEN=
# Single recipient for the local TUI, which has no accounts database. Holds a
# Telegram chat id (or, under an SMS channel, a phone number). The web app
# ignores this and uses each account's stored messaging contact instead.
AIME_MESSAGING_CONTACT=

# Host ports the Docker containers publish on. Both stay bound to 127.0.0.1
# (host-loopback only); only the port number changes. Override if 5000/5050
# are already taken on the host.
AIME_HOST_PORT=5000
AIME_DASHBOARD_HOST_PORT=5050

# Which Whisper speech-to-text models the Docker image should pre-download
# at build time. Only downloaded models appear as options in the voice-input
# settings dropdown. Tiny is on by default to keep the image small; turn the
# others on if you want better accuracy out of the box.
#   tiny.en   ~75MB,  fastest, lower accuracy
#   base.en   ~145MB, fast
#   small.en  ~480MB, balanced
# Heavier models (medium, large) still work — faster-whisper fetches them on
# first use into the models volume.
DOWNLOAD_TINY=1
DOWNLOAD_BASE=0
DOWNLOAD_SMALL=0
